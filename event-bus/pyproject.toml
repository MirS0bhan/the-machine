[tool.poetry]
name = "event-bus"
version = "0.1.0"
description = "L1 Event/Scheduler Bus — reactive routing, timers, and agent-wake decisions"
authors = ["The Machine Team"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.5.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.4.0"
pytest-asyncio = "^0.23.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
